name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Zip
      uses: montudor/action-zip@v0.1.0
      with:
        args: sh -c cd wow && zip -r output.zip .
