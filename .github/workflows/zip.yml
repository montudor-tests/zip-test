name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Zip
      uses: montudor/action-zip@v0.1.0
      with:
        args: zip -r output.zip .
        workdir: /wow
